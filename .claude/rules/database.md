# Database Rules

If sample datasets are needed for API testing, keep them under:

- `result/<slug>/07-data/`

Do not generate product database schema or migrations in this repo unless explicitly requested.

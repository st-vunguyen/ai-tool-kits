---
agent: agent
description: "Set up the performance tooling, execute the performance workload generated by prompt 19, collect raw evidence, and publish a curated performance report."
tools: ['search', 'edit', 'new', 'todos', 'runSubagent', 'problems', 'changes', 'runCommands', 'runTasks']
---

# Run Full Performance Testing

Use this prompt after `18-performance-scenarios.prompt.md` and `19-performance-collection.prompt.md` have already produced a workload model and performance artifact.

## Role

You are a **Performance Execution Engineer + API Reliability Analyst + Safe-Environment Gatekeeper**.

## Goal

Verify the generated performance assets, set up the required tooling, execute a safe and evidence-backed performance run, store raw outputs under the canonical raw-report path, and publish a curated report that clearly separates measured facts from assumptions and tooling limitations.

## Inputs

- `API_SPEC_PATH`
- `DOCS_PATHS`
- `OUTPUT_SLUG`
- `TARGET_STACK`: e.g. `postman-newman`, `k6`, `jmeter`, or another explicitly supported stack
- `TARGET_ENV`: `local | dev | staging | perf`
- `BASE_URL`: required for the actual run
- `AUTH_MODE`: `none | bearer | api-key` (descriptive only; no secrets)
- `EXECUTION_MODE`: `baseline | load-step | stress-lite` (default `baseline`)
- `SAFE_TO_RUN`: `yes | no`
- `MAX_DURATION_MINUTES`: default `15`
- `MAX_VUS_OR_ITERATIONS`: evidence-backed upper bound only

## Preconditions

Do not start execution until all of the following are true:

- `result/<OUTPUT_SLUG>/03-scenarios/performance-scenarios.md` exists
- `result/<OUTPUT_SLUG>/03-scenarios/performance-thresholds-and-assumptions.md` exists
- `result/<OUTPUT_SLUG>/09-performance/README.md` exists
- `result/<OUTPUT_SLUG>/02-strategy/performance-collection-reporting.md` exists
- a workload artifact suitable for the chosen stack exists or can be responsibly derived from prompt `19`
- the target environment is explicitly safe for non-destructive performance execution

If any precondition is missing, refresh the missing asset first instead of inventing it.

## Canonical Output Roots

- Raw outputs: `result/<OUTPUT_SLUG>/10-reports/raw/performance/<run-slug>/`
- Curated report: `result/<OUTPUT_SLUG>/10-reports/performance/<run-slug>/`
- Refreshed execution helpers: `result/<OUTPUT_SLUG>/09-performance/`

## Required Output Files

Create or refresh at minimum:

- `result/<OUTPUT_SLUG>/09-performance/README.md`
- `result/<OUTPUT_SLUG>/09-performance/run-local.sh`
- `result/<OUTPUT_SLUG>/09-performance/run-local.ps1`
- `result/<OUTPUT_SLUG>/09-performance/local.env.example`
- `result/<OUTPUT_SLUG>/10-reports/raw/performance/<run-slug>/README.md`
- `result/<OUTPUT_SLUG>/10-reports/performance/<run-slug>/00_index.md`
- `result/<OUTPUT_SLUG>/10-reports/performance/<run-slug>/01_execution-plan.md`
- `result/<OUTPUT_SLUG>/10-reports/performance/<run-slug>/02_setup-and-tooling.md`
- `result/<OUTPUT_SLUG>/10-reports/performance/<run-slug>/03_run-results.md`
- `result/<OUTPUT_SLUG>/10-reports/performance/<run-slug>/04_findings-and-bottlenecks.md`
- `result/<OUTPUT_SLUG>/10-reports/performance/<run-slug>/05_limitations-and-next-actions.md`

If the chosen stack supports richer runtime assets, also create them under `result/<OUTPUT_SLUG>/09-performance/`, for example:

- `k6-script.js`
- `newman-performance.collection.json`
- `newman-performance.postman_environment.json.example`
- `execution-config.json`

If `TARGET_STACK=jmeter` and the repo needs the full standard JMeter workflow, continue with prompts `23` through `26` after this baseline execution path or switch directly to that branch when JMeter is the primary runner.

## Guardrails

- This prompt is an execution template and must not store generated run output inside itself.
- Do not overwrite files under `.github/prompts/` or `.claude/prompts/`.
- Do not run destructive workloads against production.
- Do not commit real secrets, tokens, or current-value exported environments.
- If the selected stack is only partially suitable for performance testing, label the run as constrained and document the limitation explicitly.

## Anti-Hallucination Rules

- Do not invent throughput targets, latency SLAs, or error budgets.
- Do not claim load-test realism when the stack only supports lightweight iteration sampling.
- Do not claim bottlenecks without measured evidence from the run outputs.
- If system telemetry is unavailable, write `Needs infrastructure validation` instead of guessing server-side causes.

## Execution Method

### 1. Validate the generated performance plan

1. Read the outputs from prompts `18` and `19` in full.
2. Confirm the selected endpoints, workload model, thresholds, assumptions, and blockers are traceable.
3. If `TARGET_STACK=postman-newman`, classify the run as a constrained API workload check unless a dedicated performance runner artifact is also available.

### 2. Prepare the execution toolchain

1. Determine the actual runtime command for the chosen stack.
2. Create or refresh local runner scripts and environment examples.
3. Document prerequisites in `result/<OUTPUT_SLUG>/09-performance/README.md`.
4. Ensure raw outputs will land only under `result/<OUTPUT_SLUG>/10-reports/raw/performance/<run-slug>/`.

### 3. Enforce a safe run gate

Only continue if all are true:

- `SAFE_TO_RUN=yes`
- environment is isolated or explicitly approved for performance execution
- workload is non-destructive or cleanup is documented
- concurrency / iteration ceilings are documented and respected

If the gate fails, do not execute the run. Produce the report with `Blocked` status and explain why.

### 4. Execute in stages

Run in this order:

1. **Smoke calibration** — verify auth, variable binding, and endpoint reachability
2. **Baseline run** — collect measured latency, status distribution, and error rate
3. **Step-up load** — only if baseline is stable and the environment remains safe
4. **Stress-lite run** — optional and only when explicitly justified

After each stage:

- store raw outputs in the canonical raw-report folder
- update the curated report with facts, failures, and limitations
- stop escalation if error rate, safety concerns, or environmental instability exceeds the approved boundary

### 5. Publish evidence-backed findings

In the final report:

- separate measured results from assumptions
- separate performance findings from tooling limitations
- state whether the run is representative, constrained, or blocked
- list exact commands, environment shape, and workload parameters used

## Quality Gates

| Phase | Gate criteria |
|---|---|
| Asset validation | Prompts `18` and `19` outputs exist and are internally consistent |
| Tool setup | Runner scripts, env example, and canonical raw output path exist |
| Safe run gate | Environment approval, ceilings, and non-destructive rules are explicit |
| Execution | At least a calibration and baseline run completed, or a documented block explains why not |
| Reporting | Raw outputs are linked and curated findings clearly distinguish facts, assumptions, and limits |

## Constraints

- Do not fabricate application monitoring data you do not have.
- Do not redefine the product's SLA without evidence.
- Do not store bulky raw outputs outside the canonical `result/<OUTPUT_SLUG>/10-reports/raw/` tree.
- Do not modify product source code as part of performance execution.
- Keep all generated execution evidence inside the canonical `result/<OUTPUT_SLUG>/09-performance/` and `result/<OUTPUT_SLUG>/10-reports/` roots only.

## Self-Check

- [ ] Prompt `19` output was reviewed before execution
- [ ] Runner setup files exist under `result/<OUTPUT_SLUG>/09-performance/`
- [ ] Raw outputs exist under `result/<OUTPUT_SLUG>/10-reports/raw/performance/<run-slug>/` or the run is explicitly blocked
- [ ] Curated report exists under `result/<OUTPUT_SLUG>/10-reports/performance/<run-slug>/`
- [ ] Measured evidence is separated from assumptions and tooling limits

## Final Response

```markdown
## Performance Execution Summary
- **Stack**: [selected stack and whether it is full-performance-capable or constrained]
- **Environment**: [target env and safety status]
- **Preparation**: [runner files created or refreshed]
- **Execution stages**: [calibration / baseline / step-up / stress-lite status]
- **Measured results**: [latency, error rate, throughput, or constrained equivalents]
- **Findings**: [confirmed observations only]
- **Limitations**: [tooling, environment, observability, or approval limits]
- **Raw outputs**: `result/<OUTPUT_SLUG>/10-reports/raw/performance/<run-slug>/`
- **Report**: `result/<OUTPUT_SLUG>/10-reports/performance/<run-slug>/`
```

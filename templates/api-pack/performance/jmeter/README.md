# JMeter Starter

JMeter assets belong under `result/<slug>/09-performance/jmeter/`.

## Report destinations

- Raw JMeter outputs: `result/<slug>/10-reports/raw/performance/jmeter/<run-slug>/`
- Curated analysis: `result/<slug>/10-reports/performance/jmeter/<run-slug>/`
